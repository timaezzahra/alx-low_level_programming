#include <stdio.h>

/**
  * main - print alphabets in lowercase
  *
  * Return: 0 (success)
  */
int main(void)
{
	int nb;
		for (nb = 0; nb < 10; nb++)
	{
		putchar('0' + nb);
	}
		putchar('\n');
return (0);
}
